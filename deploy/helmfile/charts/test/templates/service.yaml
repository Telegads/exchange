apiVersion: v1
kind: Service
metadata:
  name: exchange-{{.Environment.Name}}
spec:
  selector:
    name: exchange-{{.Environment.Name}}
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
